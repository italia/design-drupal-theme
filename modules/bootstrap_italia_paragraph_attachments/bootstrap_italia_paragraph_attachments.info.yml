name: "Bootstrap Italia Paragraph Attachments"
description: This module manages the attachments through the paragraph module.
package: "Bootstrap Italia"

type: module
core_version_requirement: ^9 || ^10

dependencies:
  - drupal:media
  - drupal:media_library
  - bootstrap_italia:bootstrap_italia_paragraph

components:
  namespaces:
    # Components library 2.0
    bi-bcl:
      - ../../components/components-2
