name: "Bootstrap Italia Paragraph Map"
description: This module manages the map component through the paragraph module. This module uses leaflets and open street maps.
package: "Bootstrap Italia"

type: module
core_version_requirement: ^9 || ^10
dependencies:
  - drupal:geofield
  - drupal:leaflet
  - drupal:bootstrap_italia_paragraph

components:
  namespaces:
    # Components library 2.0
    bi-bcl:
      - ../../components/components-2
