{# Render output #}
{% include '@bootstrap_italia_components/modal/modal.twig' with {
  modal_id: modal_id,
  modal_header: modal_header,
  modal_close: modal_close,
  modal_body: modal_body,
  modal_dismiss_txt: modal_dismiss_txt
} %}