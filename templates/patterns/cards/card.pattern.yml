# This is the unique pattern ID.
#
# It is possible to define multiple patterns in a YML file, each with it's own
# unique ID. However, I personally think that it is more logical to have one
# pattern per file to keep them well-separated and organized.
#
# You can however, group similar patterns in a file.
# Example:
#   - button
#   - button_with_icon
#
card:
  # The label and description are used in the Drupal UI and the pattern library.
  label: 'Card'
  description: Un contenitore di contenuti con molte opzioni e varianti. https://italia.github.io/bootstrap-italia/docs/componenti/card/
  variants:
    simple:
      label: Card semplice
      description: Default simple card.
    simple_article:
      label: Articolo semplice
      description: Esempio di card contenente categorizzazione e data, firma dell’articolo e link all’articolo.
    with_icon:
      label: Card con icona
      description: Esempio di card contenente categorizzazione e icona.
    with_shadow:
      label: Card con ombra
      description: Esempio di card con ombra.
    teaser:
      label: Card Teaser
      description: Per creare Card con contenuti brevi o di “anteprima”.
    with_img:
      label: Card con immagine
      description: La card con immagine è contraddistinta dalla classe .card-img applicata al div .card.
  # Fields of data supported by the pattern.
  # This can include optional fields which you can handle in your TWIG.
  fields:
    card_title:
      type: text
      label: Title
      description: The title of the card.
      preview: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor…'
    card_subtitle:
      type: text
      label: SubTitle
      description: Sottotitolo
      preview: 'Subtitle'
    card_text:
      type: text
      label: Text
      description: Text of the card.
      preview: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.'
    card_category:
      type: text
      label: Category
      description: Top Category
      preview: 'Category name'
    card_category_url:
      type: text
      label: Category URL
      description: Top Category URL
      preview: '#'
    card_date:
      type: text
      label: Date
      description: Date format 'dd/mm/yyyy' or 'dd mmmmmm' with card with img.
      preview: '26/05/2021'
    card_signature:
      type: text
      label: Signature
      description: Firma dell'articolo
      preview: 'di Federico De Paolis'
    card_readmore_url:
      type: text
      label: Read more
      description: read more link
      preview: '#'
    card_icon:
      type: text
      label: Card icon
      description: Icona sulla card
      preview: 'it-file'
    card_simple_link:
      type: text
      label: Simple link
      description: Link semplice.
      preview: 'Simple link'
    card_simple_link_url:
      type: text
      label: Simple link URL
      description: URL del Link semplice.
      preview: '#'
    card_border_bottom:
      type: bool
      label: Bordo inferiore
      description: aggiunge un bordocolorato in basso
      preview: true
    card_big:
      type: bool
      label: Card grande
      description: Card di dimensioni maggiori.
      preview: false
    card_flag_icon:
      type: bool
      label: Icona bandierina
      description: Aggiunge una bandierina (flag) in all’articolo.
      preview: true
    card_etichetta:
      type: string
      label: Etichetta SX Categoria
      description: Categoria posta a SX con icona quando presente la bandierina.
    card_no_bottom_space:
      type: bool
      label: Togli spazio in basso.
      description: Se si vuole ridurre lo spazio in fondo alla card.
    card_shadow:
      type: bool
      label: Ombra su card teaser
      description: Effetto ombra
      preview: true
    card_rounded:
      type: bool
      label: Bordi rotondi su card teaser
      description: Card con spigoli arrotondati.
      preview: true
    card_img_responsive:
      type: string
      label: Immagine (tag HTML completo)
      description: Immagine responsive
      preview: '<img src="https://via.placeholder.com/310x190/0066cc/FFFFFF/?text=IMMAGINE%20DI%20ESEMPIO" title="img title" alt="imagealt">'
    card_img_pan:
      type: bool
      label: Immagine panoramica
      description: Immagine più bassa per effetto panorama.
  # Optional: Attached libraries.
  #
  # You can include libraries defined by other modules / themes with dependencies.
  #
  # These libraries are also available for include in theme libraries as
  # dependencies. Doing this ensures that the CSS / JS for the pattern are loaded
  # irrespective of whether a pattern of that type appears on the page.
  #   Example: ui_patterns/button.button
  # libraries:
  #   -
  #     card_simple:
  #       css:
  #         component:
  #           css/card_simple.css: {}

  # Optional: Specify a TWIG template with 'use'.
  #
  #   Example:
  #     use: 'themes/custom/THEME/patterns/blockquote/blockquote.html.twig'
  #
  # This usually contains a path relative to the Drupal root. Some documentation
  # says that a theme-relative path can also be used, but it didn't work for me.
  #
  #   Example:
  #     use: '@oldie/patterns/blockquote/blockquote.html.twig' (didn't work)
  #
  # Alternatively, you can use the 'components' module to have better-looking
  # and flexible paths.
  #
  #   Examples:
  #     use: '@patterns/blockquote/blockquote.html.twig'
  #     use: '@blockquote/blockquote.html.twig'
  #
  # Not defining 'use' makes the UI Patterns module look for a template with
  # the name 'pattern-PATTERN.html.twig. Underscores are changed into dashes.
  #
  #   Example: pattern-blockquote.html.twig
