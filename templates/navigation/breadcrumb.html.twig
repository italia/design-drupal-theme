{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
{% include '@bootstrap_italia_components/breadcrumb/breadcrumb.twig' with {
  darkbkg: darkbkg,
  image: image,
  separator: separator,
  breadcrumb: breadcrumb
} %}
{% endif %}